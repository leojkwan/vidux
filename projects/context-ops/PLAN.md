# Context Ops

## Purpose
Keep the Codex automation control plane legible, recoverable, and synchronized across the shared `ai` automation repo, the live local scheduler, and the canonical public `vidux` repo. This lane owns control-plane authority-store integrity, orchestrator self-audits, and small evidence-backed automation-fleet repairs. It does not own product delivery work inside downstream repos.

## Evidence
- [Source: `/Users/leokwan/Development/ai/automations/codex-automation-orchestrator/automation.toml`, read 2026-04-06] The shared orchestrator prompt still referenced a `projects/context-ops` store under `ai/skills/vidux`, but that store no longer existed after the Vidux canonicalization move.
- [Source: `/Users/leokwan/Development/ai/automations/codex-automation-orchestrator/memory.md`, read 2026-04-06] The last 3 orchestrator notes show repeated repo-vs-DB cleanup work on DB-only rows and prompt drift, but no surviving public Context Ops store for the orchestrator itself.
- [Source: `/Users/leokwan/Development/ai/automations/README.md`, read 2026-04-06] The ai repo is still the shared source of truth for automation definitions, while control-plane prompts may reach into the public `vidux` repo when the slice improves the portable Layer 1 surface.
- [Source: `~/.codex/sqlite/codex-dev.db` table `automations`, queried 2026-04-06] Repo-backed shared IDs and live scheduler rows currently match on `status`, `rrule`, `model`, and `reasoning_effort`, so the highest-leverage defect in this cycle is missing authority-store surface, not cadence/model drift.
- [Source: `/Users/leokwan/Development/ai` + `origin/main`, checked 2026-04-06] `git rev-list --left-right --count HEAD...origin/main` returned `0 0`, but `git pull --rebase` is blocked by unrelated unstaged changes in the ai repo, so control-plane edits must stay narrowly scoped.
- [Source: `evidence/2026-04-06-context-ops-store-recovery.md`] The public Vidux repo did not contain `projects/context-ops/` at all after canonical unification, even though the ai-hosted orchestrator still depended on it.
- [Source: `evidence/2026-04-06-completion-honesty-audit.md`] StrongYes and Resplit were both overstating completion for different control-plane reasons: StrongYes had a cold release queue plus `Recommendation: promote` but no remaining-mission-gap inventory, while Resplit recurring prompts still pointed at a vanished public Vidux store and were therefore over-indexing on repo-local slice and release-boundary truth.
- [Source: `evidence/2026-04-06-resplit-historical-path-drift.md`] After the active Resplit prompts were fixed, stale legacy path copy still remained in the paused repo-backed `resplit-oversight` definition and the shared automation README. That copy is low-risk but still control-plane drift because it advertises a dead authority path after public-store recovery.
- [Source: `evidence/2026-04-06-missing-automation-root.md`] The live machine still points both Codex automation roots at `/Users/leokwan/Development/ai/automations`, but the shared tree is missing or only partially present in the worktree and the repo is also one commit behind `origin/main`, so this run cannot safely claim filesystem repo truth or mutate DB rows as if repo sync were healthy.
- [Source: `evidence/2026-04-06-orchestrator-authority-drift.md`] The current machine now shows `27` repo-backed automation definitions on disk and `33` live scheduler rows with zero shared-field or prompt mismatches across the `27` shared IDs. The active drift is the orchestrator's own authority chain plus a `6`-row DB-only StrongYes cluster that still sits outside repo truth.
- [Source: `evidence/2026-04-06-dirty-checkout-escalation.md`] The last 3 orchestrator notes now repeat the same dirty-checkout pull failure in `/Users/leokwan/Development/ai`, but the shared `27` repo-backed rows still match live SQLite exactly, so the blocker is now an environment-sync problem rather than fresh fleet drift.
- [Source: `evidence/2026-04-06-db-only-timestamp-normalization.md`] The `7` remaining DB-only active rows were malformed rather than merely undocumented: all were stamped with second-based `created_at`/`updated_at` values in a millisecond scheduler schema, which left them `ACTIVE` but unarmed. The cluster is now normalized in SQLite and the public rebuild script no longer writes second-based `updated_at`.
- [Source: `evidence/2026-04-06-db-only-stall-detection.md`] Timestamp normalization was not enough to restore dispatch: the same `7` DB-only active rows are now overdue by `30-48` minutes with `0` runs, and the public `vidux-doctor.sh` needed a new scheduler-row audit so this hidden stall stops masquerading as fleet health.
- [Source: `evidence/2026-04-06-strongyes-release-memory-contract.md`] `strongyes-release-train` was carrying `7` retained checkpoints in `memory.md` even though the harness only needs the latest sibling context plus the current blocked `T70` release lane. That is active control-plane drift because stale memory weight can outlive the exact blocker it is supposed to describe.
- [Source: `evidence/2026-04-06-strongyes-content-memory-contract.md`] `strongyes-content` still read both self and sibling memory only from `$CODEX_HOME/...` even though `CODEX_HOME` is unset on this machine, and the shared live memory file had grown to `10` retained notes / `11557` bytes even though the latest cold-state notes were repeating the same Nia + ownership boundary.
- [Source: `evidence/2026-04-06-strongyes-revenue-memory-contract.md`] `strongyes-revenue-radar` still read self and release-train memory only from `$CODEX_HOME/...` even though `CODEX_HOME` is unset on this machine, and the shared live memory file had drifted to `4` retained notes even though the lane should keep a rolling last-3 window.
- [Source: `evidence/2026-04-06-strongyes-ux-memory-contract.md`] `strongyes-ux-radar` was still the remaining active StrongYes radar whose harness read self and release-train memory only from `$CODEX_HOME/...` even though `CODEX_HOME` is unset here, and its shared live memory file had already drifted to `4` retained notes despite the lane only needing the latest 3 checkpoints.

## Constraints
- ALWAYS: Keep one bounded control-plane improvement per run.
- ALWAYS: Sync repo-backed prompt truth and the live scheduler prompt together when the prompt changes.
- ALWAYS: Preserve unrelated dirty work in both `ai` and `vidux`; scope edits tightly.
- NEVER: Turn this lane into product implementation work for downstream repos.
- NEVER: change live scheduler state without writing the reason into this store first.

## Decisions
- [2026-04-06] Decision: The canonical Context Ops store now lives in `/Users/leokwan/Development/vidux/projects/context-ops/`, while the scheduler definition remains in `/Users/leokwan/Development/ai/automations/`. Rationale: the public Vidux repo owns the portable meta-lane surface, while the ai repo owns the local Codex scheduler contract.
- [2026-04-06] Decision: The orchestrator prompt may read the repo-backed memory file when `$CODEX_HOME` is unavailable. Rationale: this machine's current automation root is a symlink to `/Users/leokwan/Development/ai/automations`, and the shell used by the automation can lack a populated `$CODEX_HOME`.
- [2026-04-06] Decision: Missing authority-store files are higher priority than another round of DB-only row classification. Rationale: the fleet audit loop cannot self-audit reliably if its own first-read chain is broken.
- [2026-04-06] Decision: Recurring automation copy must separate lane or release health from overall mission completion. Rationale: a cold queue or releasable build can be true while the user-visible product is still far from complete.
- [2026-04-06] Decision: When the live automation root symlinks target a missing shared directory, treat root recovery as the blocker before any further live scheduler sync edits. Rationale: row-level fixes are not trustworthy while the machine's declared repo authority path is broken.
- [2026-04-06] Decision: Until an explicit DB-only cutover is shipped and verified, treat `/Users/leokwan/Development/ai/automations` plus `~/.codex/sqlite/codex-dev.db` as dual live truth. Rationale: the current machine still has a populated repo-backed automation tree and matching scheduler rows, while the DB-only retirement story in `vidux/PLAN.md` was premature.
- [2026-04-06] Decision: After the same dirty-checkout pull failure appears in the last 3 orchestrator notes, treat it as a persistent environment blocker and stop spending the bounded improvement slot rediscovering it. Rationale: repeated pull-failure reporting creates churn but adds no new control-plane information once the shared rows are already verified clean.
- [2026-04-06] Decision: Do not treat freshly seeded `next_run_at` as proof that a DB-only fleet is healthy. Rationale: scheduler health requires a doctor-visible run-history check for active overdue zero-run rows, otherwise the control plane over-claims recovery from timestamp-only fixes.
- [2026-04-06] Decision: Treat oversized active automation memory files as control-plane drift when the harness already expects a rolling last-3-note window. Rationale: stale retained notes create prompt-state weight without adding new authority once the active lane and blocker are already represented in the latest checkpoints.
- [2026-04-06] Decision: Watch-mode lanes must use an explicit real memory path plus rolling-last-3 retention, and unchanged `next_action=burst` handoffs should compress rather than accrete synonym churn. Rationale: repeated hot-state summaries create memory drift without adding new queue truth when the plan and proof bundle are unchanged.
- [2026-04-06] Decision: Read-mostly StrongYes radars must use the same runtime-memory fallback contract as the writer lane: `$CODEX_HOME/...` when set, otherwise `~/.codex/...`, with only the last 3 notes retained. Rationale: otherwise cold-state radars can rehydrate from missing or stale memory even while the live shared automation root already carries fresher checkpoints.

## Tasks
- [completed] Task 1: Recover the missing public Context Ops store after Vidux canonicalization and repoint the shared orchestrator prompt to current authority files. [Evidence: `evidence/2026-04-06-context-ops-store-recovery.md`, `/Users/leokwan/Development/ai/automations/codex-automation-orchestrator/automation.toml`, `~/.codex/sqlite/codex-dev.db`] [Done: 2026-04-06]
- [completed] Task 2: Audit the remaining active automation prompts that still depend on ai-hosted Vidux project-store paths and decide which stores must move into public `vidux`, which should stay ai-local, and which should stop depending on Vidux path indirection entirely. [Evidence: `evidence/2026-04-06-completion-honesty-audit.md`, `/Users/leokwan/Development/ai/automations/resplit-nurse/automation.toml`, `/Users/leokwan/Development/ai/automations/resplit-vidux/automation.toml`, `/Users/leokwan/Development/ai/automations/strongyes-release-train/automation.toml`, `~/.codex/sqlite/codex-dev.db`] [Done: 2026-04-06]
- [pending] Task 3: Continue the fleet-wide audit for other loops that can still mistake cold lane state, release status, or missing authority files for whole-mission completion. [Evidence: `/Users/leokwan/Development/ai/automations/*/automation.toml`, `evidence/2026-04-06-completion-honesty-audit.md`]
- [completed] Task 4: Repair the orchestrator's own authority chain after the premature DB-only retirement story by removing dead file dependencies, documenting the real SQLite schema, and re-syncing the live prompt. [Evidence: `evidence/2026-04-06-orchestrator-authority-drift.md`, `/Users/leokwan/Development/ai/automations/codex-automation-orchestrator/automation.toml`, `~/.codex/sqlite/codex-dev.db`] [Done: 2026-04-06]

## Decision Log
- [DIRECTION] [2026-04-06] Treat public Vidux packaging gaps as control-plane bugs when they break a live automation's authority chain.
- [DIRECTION] [2026-04-06] Fix the orchestrator itself before widening the scope to other fleet lanes.
- [DIRECTION] [2026-04-06] If `~/.codex/automations` points at a missing directory, document and escalate root recovery before rewriting overdue scheduler rows.
- [DIRECTION] [2026-04-06] When retrospective notes disagree with the current automation tree and live SQLite, trust the current filesystem + DB and correct the stale note instead of brute-forcing more row churn.

## Progress
- [2026-04-06] Cycle 1: Re-read the orchestrator TOML, repo-backed memory, `automations/README.md`, live scheduler rows, and public Vidux repo status. Found that `ai/skills/vidux` is now a symlink into `/Users/leokwan/Development/vidux`, so the skill path itself survives, but `projects/context-ops/` had vanished during canonical unification. Created this public Context Ops store, added the recovery evidence note, repointed the orchestrator prompt to the new canonical files plus a `$CODEX_HOME` fallback for memory, and synced the live scheduler prompt back to repo truth. outcome=useful. Next: Task 2, the broader fleet audit of remaining Vidux project-store path dependencies. Blocker: none.
- [2026-04-06] Cycle 2: Audited the StrongYes and Resplit recurring prompts after the user's false-completion report. Found a shared control-plane bug with two causes: StrongYes was treating a releasable cold queue as implicit product completion because the store carried no remaining-mission-gap inventory, while Resplit recurring prompts still referenced a missing public Vidux store and were therefore collapsing broader mission state into repo-local slice and release-boundary truth. Added the completion-honesty audit note, recovered the public Resplit store, patched the StrongYes plan/harness plus the affected ai automation prompts, and synced the live scheduler prompt rows back to repo truth. outcome=useful. Next: Task 3, continue the fleet-wide completion-honesty audit beyond these two product lanes. Blocker: none.
- [2026-04-06] Cycle 3: Re-read the fleet audit against the real SQLite schema and found one remaining tracked repo/DB mismatch: the paused historical `resplit-oversight` row still pointed at the legacy `ai/skills/vidux/projects/resplit/...` path, and the shared automation README advertised the same dead store even though the active Resplit loops were already on the public Vidux path. Added a historical-path-drift evidence note, normalized the repo-backed paused definition plus README to `/Users/leokwan/Development/vidux/projects/resplit/...`, and synced the live SQLite prompt for `resplit-oversight` back to repo truth. outcome=useful. Next: investigate why `vidux-endurance` and `vidux-v230-planner` remain `ACTIVE` with past `next_run_at` and zero `automation_runs`, then decide whether the right fix is scheduler re-arming, archive cleanup, or deeper runtime diagnosis. Blocker: none.
- [2026-04-06] Cycle 4: Audited the cold active Vidux rows and found a more fundamental blocker first: the live machine still points both Codex automation roots at `/Users/leokwan/Development/ai/automations`, but that shared tree was missing at audit start and is still only partially present after recreating the orchestrator memory path, while the repo is also one commit behind `origin/main`. Added the missing-root evidence note and chose not to mutate `vidux-endurance` or `vidux-v230-planner` in SQLite yet, because repo-vs-DB sync claims are not trustworthy until the shared root is recovered or intentionally relocated. outcome=useful. Next: recover or deliberately repoint the shared automation root, then rerun the overdue-row diagnosis with a trustworthy repo source. Blocker: shared automation root missing or partial in live worktree.
- [2026-04-06] Cycle 5: Re-ran the control-plane audit against the current filesystem, repo status, and live SQLite schema after the earlier missing-root blocker. Found the shared automation tree is in fact populated with `27` repo-backed definitions and the live DB now shows `33` rows with a `6`-row DB-only StrongYes cluster and zero shared-field or prompt drift across the `27` shared IDs. The real self-drift was the orchestrator's missing `automations/README.md` dependency, the stale public DB-only retirement note in `vidux/PLAN.md`, and outdated schema assumptions. Added the authority-drift evidence note, corrected the public plan, tightened the orchestrator prompt, and synced the live scheduler prompt row. outcome=useful. Next: classify whether the DB-only StrongYes cluster should be archived, documented, or promoted into repo truth, especially because all six rows currently have null `next_run_at`. Blocker: `git pull --rebase` in `/Users/leokwan/Development/ai` still fails because of unrelated local changes.
- [2026-04-06] Cycle 6: Re-audited current repo truth versus live SQLite and found the fleet count has shifted again: `27` repo-backed TOMLs, `34` scheduler rows, and `7` DB-only rows (`6` StrongYes burst lanes plus `vidux-meta`). The bounded live defect was prompt-header drift in two active repo-backed automations: `strongyes-release-train` and `vidux-v230-planner` had both lost their explicit `[$vidux]` activation line even though they remain active in SQLite. Added `evidence/2026-04-06-active-prompt-header-drift.md`, restored the public Vidux skill link in both repo-backed TOMLs, and synced the matching SQLite prompt rows without changing cadence or model. outcome=useful. Next: repair missing or malformed memory contracts for active repo-backed loops, starting with `strongyes-release-train`, `resplit-asc`, and `resplit-currency`. Blocker: `git pull --rebase` in `/Users/leokwan/Development/ai` still fails because of unrelated local changes.
- [2026-04-06] Cycle 7: Re-read the last 3 orchestrator notes, current repo-vs-DB sync state, and canonical repo status before picking the next slice. Shared control-plane truth is still clean (`27` repo-backed IDs, `34` scheduler rows, `0` shared-field mismatches, `7` DB-only active rows), but the same `git pull --rebase` failure is now repeated across all 3 recent notes and the canonical `ai` checkout is also `0 1` behind upstream with large unrelated staged deletions and modifications. Added `evidence/2026-04-06-dirty-checkout-escalation.md` and escalated the blocker as process truth instead of spending another bounded improvement slot rediscovering it. outcome=queued. Next: once a deliberate sync window restores a trustworthy `/Users/leokwan/Development/ai` checkout, resume DB-only row classification starting with the `6` StrongYes burst lanes plus `vidux-meta`. Blocker: canonical `ai` checkout is dirty and one commit behind `origin/main`.
- [2026-04-06] Cycle 8: Re-read the Context Ops store, canonical repo-vs-DB sync state, and the DB-only cluster instead of revisiting the already-escalated dirty checkout. Found that the `7` remaining DB-only active rows were malformed rather than merely undocumented: all had second-based `created_at`/`updated_at` values (`1775517838`), null `next_run_at`, and zero runs in a millisecond scheduler schema. Added `evidence/2026-04-06-db-only-timestamp-normalization.md`, patched `scripts/vidux-fleet-rebuild.sh` to write millisecond `updated_at`, and normalized the live rows so they now carry future dispatch times again without changing cadence/model/cwd truth. outcome=useful. Next: decide whether the now-armed DB-only StrongYes burst lanes plus `vidux-meta` should remain intentional local-only automations, be promoted into repo truth, or be retired. Blocker: canonical `ai` checkout is still dirty and one commit behind `origin/main`.
- [2026-04-06] Cycle 9: Re-read the Context Ops store, live repo-vs-DB diff, automation run history, and one ledger slice before choosing the next move. Found that the same `7` DB-only active rows are now `30-48` minutes overdue with `0` runs even after the timestamp normalization, while unrelated Codex live work still appears in `strongyes-web` and `vidux`; the real gap was that `vidux-doctor.sh` had no check for overdue zero-run active rows. Added `evidence/2026-04-06-db-only-stall-detection.md`, extended the public doctor with a `stalled_active_automation_rows` audit plus `--automation-db`, added a contract test, and verified the live doctor now surfaces the real `7`-row cluster. outcome=useful. Next: classify whether those `7` DB-only rows are intentionally local-only but scheduler-ineligible, or whether the DB-only rebuild path still misses a required live-scheduler write. Blocker: canonical `ai` checkout is still dirty and direct row churn on the same cluster already failed to restore dispatch.
- [2026-04-06] Cycle 10: Re-read the Context Ops store, the last 3 orchestrator notes, repo-vs-DB sync state, and active shared memory files before choosing the next slice. Shared scheduler truth is still clean for the `27` repo-backed IDs, while `strongyes-release-train` carried `7` retained checkpoints even though the harness only needs a rolling last-3 window and the lane is still blocked on the same operator-owned `T70` domain re-home. Added `evidence/2026-04-06-strongyes-release-memory-contract.md`, tightened the shared harness so sibling and self-memory reads fall back to `~/.codex` when `$CODEX_HOME` is unset and the release-train memory must keep only the last 3 notes, trimmed the live shared memory file, and synced the SQLite prompt row back to repo truth. outcome=useful. Next: normalize the next oversized active memory file one lane at a time, unless a stronger authority source appears for the stalled `7` DB-only rows. Blocker: canonical `ai` checkout is still dirty and one commit behind `origin/main`, so broader fleet sync remains an environment issue rather than a row-level mismatch.
- [2026-04-06] Cycle 11: Re-read the Context Ops store, current fleet inventory, live prompt lengths, automation run counts, and shared memory sizes before choosing the next slice. Shared scheduler truth is still clean for the `27` repo-backed IDs against `34` live rows, but `strongyes-content` still depended on `$CODEX_HOME/...`-only memory paths even though `CODEX_HOME` is unset here, and its shared memory had grown to `10` retained notes / `11557` bytes while repeating the same cold-state Nia and ownership boundary. Added `evidence/2026-04-06-strongyes-content-memory-contract.md`, tightened the shared harness so self and sibling memory reads fall back to `~/.codex` and the content-radar memory must keep only the last 3 notes, trimmed the live shared memory file, and synced the SQLite prompt row back to repo truth. outcome=useful. Next: normalize the next oversized active shared memory file one lane at a time, with `resplit-nurse`, `resplit-asc`, and `strongyes-flow-radar` now the clearest candidates unless the stalled `7` DB-only rows become actionable. Blocker: canonical `ai` checkout is still dirty and one commit behind `origin/main`, so this remains a live-root patch instead of a clean git-sync pass.
- [2026-04-06] Cycle 12: Re-read the authority store, active repo-vs-DB inventory, mounted automation root, and active memory files after correcting a false positive from the stale worktree copy. Shared scheduler truth still shows `27` repo-backed IDs against `34` live rows with `7` DB-only rows, but `vidux-endurance` was the live public-meta outlier: its harness only referenced an ambiguous relative `memory.md`, the live memory file had grown to `6` retained entries, and `4` consecutive runs were restating the same unchanged Project 10 `next_action=burst` handoff. Added `evidence/2026-04-06-vidux-endurance-memory-contract.md`, tightened the shared harness so the lane reads memory from the real automation root with explicit fallback and keeps only the last 3 concise timestamped notes, trimmed the live memory file, and synced the matching SQLite prompt row back to repo truth. outcome=useful. Next: audit the next active loop whose memory still lacks an explicit fallback plus last-3 contract, with `resplit-android` currently the clearest candidate unless the DB-only `7`-row cluster becomes actionable first. Blocker: canonical `ai` checkout is still dirty and one commit behind `origin/main`, so this remains a narrow live-root patch instead of a clean git-sync pass.
- [2026-04-06] Cycle 13: Re-read the authority store, active repo-vs-DB inventory, StrongYes radar prompts, and live shared memory files before picking the next bounded slice. Shared scheduler truth still shows `27` repo-backed IDs against `34` live rows with the same `7` DB-only active rows, but `strongyes-revenue-radar` was still the only active StrongYes radar whose harness read self and release-train memory only from `$CODEX_HOME/...` even though `CODEX_HOME` is unset here, and the shared live memory file had drifted to `4` retained notes. Added `evidence/2026-04-06-strongyes-revenue-memory-contract.md`, tightened the shared harness so the lane uses `$CODEX_HOME/...` when present and otherwise `~/.codex/...`, added the explicit rolling-last-3 checkpoint rule, trimmed the live memory file, and synced the matching SQLite prompt row back to repo truth. outcome=useful. Next: audit the next active radar whose live memory contract still diverges from the now-shared StrongYes pattern, with `strongyes-ux-radar` the clearest candidate unless the DB-only `7`-row cluster becomes actionable first. Blocker: canonical `ai` checkout is still dirty, one commit behind `origin/main`, and `git fetch` now also fails on `.git/FETCH_HEAD` because the disk is effectively full, so this remains a narrow live-root patch instead of a clean git-sync pass.
- [2026-04-06] Cycle 14: Re-read the authority store, mounted live automation root, current StrongYes radar prompts, run history, and shared memory files before picking the next bounded slice. Shared scheduler truth still shows the same repo-backed fleet against `34` live rows with `7` DB-only active rows, but `strongyes-ux-radar` was now the remaining active StrongYes radar whose harness still read self and release-train memory only from `$CODEX_HOME/...` even though `CODEX_HOME` is unset here, and its live shared memory file had drifted to `4` retained notes. Added `evidence/2026-04-06-strongyes-ux-memory-contract.md`, tightened the shared harness so the lane uses `$CODEX_HOME/...` when present and otherwise `~/.codex/...` with repo fallback only as a last resort, added the explicit rolling-last-3 checkpoint rule plus delta-only restatement guidance, trimmed the live shared memory file to `3` notes, then re-trimmed once after one in-flight UX-radar run appended a final old-contract checkpoint, and synced the matching SQLite prompt row back to repo truth. outcome=useful. Next: return to fleet-level drift and decide whether the overdue `7` DB-only active rows should be documented, promoted, or retired now that the active StrongYes radar memory contracts are normalized. Blocker: canonical `ai` checkout is still dirty, one commit behind `origin/main`, and broader git-sync hygiene remains blocked by the same environment state.
